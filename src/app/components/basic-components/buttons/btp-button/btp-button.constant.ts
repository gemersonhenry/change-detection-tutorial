import { BOOTSTRAP_GENERIC_TYPES_02 } from 'src/app/components/settings/enums';

export const BUTTON_TYPES_ARRAY: BOOTSTRAP_GENERIC_TYPES_02[] = Object.keys(BOOTSTRAP_GENERIC_TYPES_02)
  .map(key => BOOTSTRAP_GENERIC_TYPES_02[key]);
